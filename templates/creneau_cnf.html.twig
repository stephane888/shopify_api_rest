{#
/**
 * @file creneau_cnf.html.twig
 * Default theme implementation to present Creneau cnf data.
 *
 * This template is used when viewing Creneau cnf pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_creneau_cnf()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('creneau_cnf') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
